# SparseMSE

**SparseMSE is deprecated.** It has been superseded by the
`MultipleSystemsEstimation` package, which should be used for current
development and support.

This release preserves the historical SparseMSE implementation while directing
new work to its successor package.
